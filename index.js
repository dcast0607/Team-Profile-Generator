// Bringing in our class definitions from the lib directory. 
const { Employee, Engineer, Intern, Manager } = require('./lib');

// Brings in the inquirer package so that we can use it.
const inquirer = require('inquirer');

// Brings in the fs package so that we can use it. 
const fs = require('fs');

